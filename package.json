{
  "name": "norns",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "configure": "emcmake cmake -DCMAKE_INSTALL_PREFIX=./ -Bbuild -H.",
    "build": "cmake --build build --target matron install",
    "start": "node ."
  }
}
